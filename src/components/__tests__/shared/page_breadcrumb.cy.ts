import PageBreadcrumb from '../../shared/page_breadcrumb.vue'

describe('components/shared/page_breadcrumb.vue', () => {
  it('renders properly with initial conditions', () => {
    cy.mount(PageBreadcrumb)
  })
})
